<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:theme="@style/Theme.TrillEcommerce">


    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="@dimen/menu_item_corner_radius"
        app:cardElevation="@dimen/menu_item_card_elevation"
        android:layout_marginStart="@dimen/menu_item_margin_start"
        android:layout_marginEnd="@dimen/menu_item_margin_end"
        android:layout_marginTop="@dimen/menu_item_margin_top"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:id="@+id/cardView">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="@dimen/menu_item_image_height"
            android:src="@drawable/art_item_placeholder"
            android:scaleType="fitCenter"
            android:padding="@dimen/menu_item_image_padding"
            android:id="@+id/imageView"
            android:contentDescription="@string/menu_title"/>
    </com.google.android.material.card.MaterialCardView>


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/title"
        android:text="Category Name"
        android:textColor="@color/text_default"
        android:maxLines="2"
        android:textSize="@dimen/menu_item_title_size"
        app:flow_wrapMode="aligned"
        android:gravity="center"
        android:layout_marginTop="5dp"
        app:layout_constraintTop_toBottomOf="@id/cardView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:fontFamily="@font/colby_bold" />

</androidx.constraintlayout.widget.ConstraintLayout>